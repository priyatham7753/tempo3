# ──────────────────────────────────────────────────────────────────────────────
# charts/product-service/templates/service.yaml
# Internal DNS: product-service.shopmesh.svc.cluster.local:3002
# ──────────────────────────────────────────────────────────────────────────────
{{- $svc := index .Values "product-service" }}
{{- $global := .Values.global }}
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: {{ $global.namespace }}
  labels:
    app.kubernetes.io/name: product-service
    app.kubernetes.io/part-of: {{ $global.partOf }}
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: product-service
  ports:
    - name: http
      protocol: TCP
      port: {{ $svc.port }}
      targetPort: {{ $svc.port }}
