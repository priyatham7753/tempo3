replicaCount: 1

image:
  tag: "latest"
  pullPolicy: IfNotPresent

port: 3001

service:
  type: ClusterIP
  port: 3001

env:
  PORT:          "3001"
  NODE_ENV:      "production"
  MONGO_DB_NAME: "authdb"
