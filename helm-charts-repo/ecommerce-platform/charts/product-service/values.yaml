replicaCount: 1

image:
  tag: "latest"
  pullPolicy: IfNotPresent

port: 3002

service:
  type: ClusterIP
  port: 3002

env:
  PORT:          "3002"
  NODE_ENV:      "production"
  MONGO_DB_NAME: "productdb"
