# ──────────────────────────────────────────────────────────────────────────────
# services/frontend/secret.yaml  — Frontend secrets (placeholder)
# Add any API keys or tokens your frontend build process needs here.
# Apply: kubectl apply -f k8s/services/frontend/secret.yaml
# ──────────────────────────────────────────────────────────────────────────────

apiVersion: v1
kind: Secret
metadata:
  name: frontend-secret
  namespace: shopmesh
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: shopmesh
    app.kubernetes.io/component: frontend
type: Opaque
data:
  # Add build-time or runtime secrets here as needed.
  # Example: API_KEY: base64encodedvalue==
  PLACEHOLDER: cGxhY2Vob2xkZXI=
