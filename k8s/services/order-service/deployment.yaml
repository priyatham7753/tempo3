# ──────────────────────────────────────────────────────────────────────────────
# services/order-service/deployment.yaml  — Order Service Deployment
#
# Image   : your-registry/order-service:latest
# Port    : 3003
# Health  : GET /health
#
# Apply: kubectl apply -f k8s/services/order-service/deployment.yaml
# ──────────────────────────────────────────────────────────────────────────────

apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: shopmesh
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/part-of: shopmesh
    app.kubernetes.io/component: backend
    app.kubernetes.io/version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: order-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: order-service
        app.kubernetes.io/part-of: shopmesh
        app.kubernetes.io/component: backend
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
      terminationGracePeriodSeconds: 30

      containers:
        - name: order-service
          image: your-registry/order-service:latest   # ← Replace with your image
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3003
              protocol: TCP

          envFrom:
            - configMapRef:
                name: order-service-config

          env:
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: order-service-secret
                  key: MONGO_URI

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 40
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
